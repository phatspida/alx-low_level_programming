#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: first integer.
 * @b: second integer.
 * Return: Integer product.
 */
int mul(int a, int b)
{
	int product;

	product = (a * b);
	return (product);
}
